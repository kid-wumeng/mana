# mana

p = point
w = width
h = height
n = number
s = scalar
v = vector
el = element
cb = callback
val = value
vec = vector
mat = matrix
buf = buffer
deg = degree
rad = radian
cfg = config
opt = option
msg = message
ctx = context
pub = publish
sub = subscribe
src = source
dst = destination
dist = distribution